sonar.projectKey=paas-cost-advisor
sonar.projectName=paas-cost-advisor

# Code lives at the repo root (not in src/), so point sources at the real dirs
sonar.sources=server.js,lib,providers,public
sonar.exclusions=node_modules/**,.git/**,.env,**/*.min.js,data/**
