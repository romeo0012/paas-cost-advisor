# Runtime environment variables (CodeNOW) - mirror of the local setup.
# PORT=3000 odpovida nasazenemu runtime portu (viz .codenow.yaml runtime.port).
# BASE_PATH je prazdny - aplikace slouzi v koreni (/), aby liveness/readiness na "/" vracely 200.
# Zadne dalsi env promenne (klic/API klic) tato aplikace nepouziva.

PORT=3000
BASE_PATH=
# Vychozi vyuziti PaaS v % pro prepočet ceny (20/40/60/80/100)
PAAS_UTILIZATION=40
